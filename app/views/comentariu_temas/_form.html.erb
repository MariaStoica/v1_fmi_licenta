<%= form_for(@comentariu_tema) do |f| %>
  <% if @comentariu_tema.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentariu_tema.errors.count, "error") %> prohibited this comentariu_tema from being saved:</h2>

      <ul>
      <% @comentariu_tema.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :tema_id %><br>
    <%= f.number_field :tema_id %>
  </div>
  <div class="field">
    <%= f.label :continut %><br>
    <%= f.text_area :continut %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
