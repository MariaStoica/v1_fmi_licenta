<%= form_for(@comentariu_fisier) do |f| %>
  <% if @comentariu_fisier.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comentariu_fisier.errors.count, "error") %> prohibited this comentariu_fisier from being saved:</h2>

      <ul>
      <% @comentariu_fisier.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :fisier_id %><br>
    <%= f.number_field :fisier_id %>
  </div>
  <div class="field">
    <%= f.label :continut %><br>
    <%= f.text_area :continut %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
